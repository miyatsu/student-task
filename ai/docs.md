接下来，做一件非常重要的事情，那就是撰写各类软件工程标准所需要的规范文档，
保存到 docs/ 文件夹下。

严格遵守现代软件工程规范，包括用户手册（从用户角度描述项目，包括如何安装、运行、操作）、
需求分析文档、架构设计、开发文档（包括如何配置所需环境，如果需要下载并安装第三方工具或框架，也都要清晰描述）技术文档、
实现文档、测试文档等。

按照软件工程规范，生成各类 UML 图，将生成的图片整合到相应的文档中，
可以使用 PlantUM 作图，PlantUM 源码与生成的图片均放入 git 记录。

文档中对源文件、其他文档、代码行、类名、数据类型等的引用要是本地可跳转的。
最上层的 README仅简要介绍该项目，并将更具体的介绍链接到相关文档，
仅起提纲挈领、引导性的作用。
这些文档中的内容不要有功能上的重复，而应该采用本地可跳转引用的方式进行指代，
防止多处地方重复，后续修改导致分叉。
另外，这些文档在后续项目开发过程中要持续不断的、同步更新。

注意，本次不改动 src/ 下源代码。