请对该项目进行全面重构，包括但不限于：

- 按现在软件工程项目规范对项目结构进行重新组织。比如，目前 `pdf-merger` 目录下有很多 `.ts`、`.mjs` 代码文件，需要重新组织；完成后，git commit。
- 根据流行的设计模式，考察源代码，是否可以使用某些典型的设计模式对代码进行重构。完成后，git commit。
- 依次检查每个源代码文件。根据 SOLID 软件开发基本原则，对源代码进行重构。比如，有的文件行数过多，或者有的函数过长，是否违反了"单一职责"原则？完成后，git commit。
- 采用现代编程语言规范 `ts`、`js`，对源码进行重构。去除过时的语言特性用法，改用简洁的、更富表现力的、更易读的语言特性。完成后，git commit。

重构迭代方案：
- 先按从上到下的顺序执行上述四项重构
- 再按从下到上的顺序执行上述四项重构
- 再按从上到下的顺序执行上述四项重构

注意事项：
- 注意：重构的目的是让系统更易维护、更可读。不要为了炫技而重构，更不要为了重构而重构。
- 注意：每次重构都要同步更新所有相关文档。
- 注意：每次重构都要在 `docs/refactor/refactor.md` 中记录重构理由、重构方案、重构前后对比。
- 注意：每次重构都要保证系统功能不被破坏，都要撰写全面的测试用例，并保证所有的用例都能通过。