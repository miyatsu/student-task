# 测试文档

为了保证这套轻量级但功能复杂的多功能文件处理逻辑保持高度可用性，应当在版本迭代之前经历以下测试。当前策略已经同时覆盖纯逻辑回归、关键应用层回归以及主干流程的人工验证。

## 1. 测试策略
鉴于系统的纯前端运行特点：
1. **组件与应用层自动化测试**：当前已经为 `src/features/files/file-utils.ts` 建立纯逻辑回归测试，并为文件列表组件、`PdfEditor` 页操作流程、`ai.ts` 运行时配置加载、`local-ocr.ts` 本地 OCR 可用性，以及服务端多 provider AI gateway fallback 建立了应用层回归测试。
2. **浏览器 E2E 原理验证**：保障 Canvas 操作不产生跨域死锁或者内存逸出（OOM）。
3. **集成验证**：重点在跨模块之间流转的文件，例如是否在不同区域依然保持正确的名字以及拓展后缀名。这需要人工 Checklist 校验。
4. **实验 / 诊断脚本**：`scripts/experiments/` 下的脚本用于环境探测、库能力验证和手工联调，不属于自动化回归测试树，也不会被 `npm test` 执行。

### 1.1 当前自动化测试入口

```bash
npm test
```

该命令当前会顺序运行以下两层自动化测试：

```bash
npm run test:logic
npm run test:app
```

- `npm run test:logic` 会运行 `src/features/files/file-utils.test.ts`，用于验证文件分类、选择切换、排序、重命名、复制、批量删除以及 Zip 导出重名处理逻辑。
- `npm run test:app` 会运行当前的 Vitest 应用层回归测试，包括：
	- `ImageFilesSection`、`PdfFilesSection`、`WordFilesSection` 的列表交互、行内上移/下移、排序头 UI、批量操作事件路由，以及图片 / Word 转 PDF 进度展示。
	- `PdfEditor` 的页删除、页抽取与“禁止删除全部页面”行为。
	- `ai.ts` 的运行时配置加载、缓存与本地 gateway 请求逻辑。
	- `server.ts` 中压缩命令生成、旧版 `.doc` 文本提取、运行时 AI / 本地 OCR 配置读取，以及 `src/server/ai.ts` 的 provider 顺序回退逻辑与 `src/server/local-ocr.ts` 的本地 OCR 调度逻辑。

兼容性说明：`npm run test:ui` 目前仍保留为 `npm run test:app` 的别名，方便沿用旧命令。

### 1.2 实验脚本与自动化测试的边界

`scripts/experiments/` 当前按用途拆分为：

- `diagnostics/`：环境、CLI、第三方库 API 与能力探测。
- `manual/`：需要本地服务或手工观察结果的联调脚本。
- `spikes/`：一次性技术验证与可行性试验。

这些脚本虽然名字里带有 `test-`，但语义上更接近“开发期验证工具”，不应与真正的自动化回归测试混放到统一的 `test/` 树中。

## 2. 人工测试大纲及执行 Check List

开发者或测试人员按以下标准开展预发布检验。

### 模块一：全局文件组织 (DND与排序)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-01    | 类型混合空降 | 同时按住一本 PDF，一张 `jpg`，一个 `docx` 后，拖放至 App 工作虚线框中。          | 文件必须顺利分发到对应的 3 个独立的（Image/PDF/Word）区块内部并提供对应缩略状态。 | [ ]  |
| TC-02    | 跨文件删除   | 在各个分类中分别选中若干文件，点击红色的清空/删除选定。                         | 仅仅只有那些 Checkbox=true 的文件在试图内消失，其余保留不变。                      | [ ]  |
| TC-03    | 名称自然排序 | 在同一分组内上传 `file1.pdf`、`file2.pdf`、`file10.pdf`，点击“NAME”排序。         | 名称排序应识别数字块，顺序显示为 `file1`、`file2`、`file10`，而不是字典序。          | [ ]  |
| TC-04    | 行内顺序微调 | 在任意分组中对中间文件连续点击“上移 / 下移”按钮。                                  | 文件应按点击方向与相邻项交换位置，且当前分组的排序状态被清空，改为手动顺序。         | [ ]  |
| TC-05    | 排序控件可发现性 | 首次进入页面，不做任何操作，观察 `NAME / DATE / SIZE` 所在区域。                 | 排序区域应呈现为带图标的胶囊按钮组，用户无需 hover 或试错也能感知“这里可以排序”。   | [ ]  |
| TC-06    | 全局排序切换 | 点击文字“SIZE (大小)”标签的排序按钮。                                            | 组内按从达到小升序排列对应的大小展示；点击第二次，改为升序或者降序，方向图标更改。 | [ ]  |

### 模块二：AI 图像超级强化处理 (`ImageEnhanceModal`)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-07    | 特大尺寸载入 | 上传并点击 4K 以上分辨率或 5MB 以上图片的魔棒特效。                              | 由于 Patch-Size 的保护，等待虽然漫长（超 30s），但浏览器不得崩溃或者显示白屏抛出异常。 | [ ]  |
| TC-08    | 滑块边界检测 | 在完成增强的对比状态下，用鼠标将游标滑至最左方边界并停止，再滑动至右侧边缘并松开。 | 左方停止时应完全展示原图（含水印），右方全幅显示增强质感。不应该因为游标脱模影响布局。  | [ ]  |
| TC-09    | 中断生命周期 | AI 模型处于 Loading 状态（旋转缓冲），立刻点击底界面的 "Cancel" 中止窗口组件。     | 等待数秒不应阻碍程序的正常销毁；模型推理的上下文被回收且在原页面内无任何不兼容假死。 | [ ]  |
| TC-10    | 图片转 PDF 进度 | 选中 3 张以上图片，点击 **Convert to PDF**，观察转换过程中的图片区块底栏。         | 应显示环形百分比、进度条、`已完成/总数` 数字与当前文件名；进度应随着每张图片完成而推进。 | [ ]  |

### 模块三：Word 转 PDF（含旧版 `.doc`）
当前默认顺序为：`local Microsoft Word -> LibreOffice CLI -> browser HTML fallback`。以下用例按这个优先级验证。

| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-11    | Word 转 PDF 进度 | 选中 3 份以上 Word 文档，点击 **To PDF**，观察 Word 区块底栏。                 | 应显示绿色进度卡片，包含环形百分比、进度条、`已完成/总数` 数字、当前文件名以及已用时长。 | [ ]  |
| TC-11A   | Word 完成态收尾 | 选中多份 Word 文档并等待全部转换完成，观察最后一份完成后的底栏状态。              | 进度卡应先显示 `100%` 与收尾文案，然后自动消失；不应再弹出成功对话框，也不应停留在 `4/5` 这类旧进度。 | [ ]  |
| TC-11B   | Word 当前方式提示 | 选中一份或多份 Word 文档执行 **To PDF**，观察转换过程中的 Word 底栏。            | 进度卡应实时显示当前采用的具体方式，例如 `LibreOffice CLI export`、`local Microsoft Word export` 或 `browser HTML fallback`。 | [ ]  |
| TC-11C   | Word -> CLI -> HTML 回退链 | 在装有 Microsoft Word 的机器上执行 **To PDF**；或在临时禁用 Word 后再次执行；最后在两者都不可用时验证兜底。 | 系统应优先尝试本地 Word；Word 不可用或失败时退回 LibreOffice CLI；两者都不可用时再退回 HTML 路径，且界面中的方式提示应同步变化。 | [ ]  |
| TC-12    | `.doc` 转 PDF | 上传一份旧版 `.doc` 文档并执行 **To PDF**。                                     | 转换流程应成功生成 PDF；若版式无法完全还原，至少正文文本应可读且不会再报“仅支持 DOCX”。 | [ ]  |
| TC-13    | Word 导出非空白页 | 分别上传一份 `.docx` 和一份 `.doc`，执行 **To PDF**，打开导出的 PDF。             | 生成的 PDF 页面不应为空白；至少第一页应能看到正文文本，证明本地隐藏渲染宿主没有再发生高度塌缩。 | [ ]  |

### 模块四：PDF 工作台互操作 (`PdfEditor`)
参阅：`[src/components/PdfEditor.tsx](../src/components/PdfEditor.tsx)`
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-14    | PDF 单页删除 | 打开编辑抽屉，点击指定页面上的叉号。                                              | 剩下页码实时重构为缩略图；点击导出生成一个只有其余页面的完全可读新 PDF 文件。   | [ ]  |

### 模块五：压缩集成与导出 (Zip 机制)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-15    | 复合混装导出 | 勾选 1 个经过重度模糊增强而复制的副本图像、1 个拆分的 PDF 和 1 份 Word 文件并打包。| 生成压缩包不报错；解包后打开 3 份文件在系统自带预览中不会由于文件头损坏而不能显示。 | [ ]  |

### 模块六：开发环境与端口冲突回归
| 用例编号 | 用例名称         | 操作动作                                                                 | 预期结果                                                                 | 状态 |
|----------|------------------|--------------------------------------------------------------------------|--------------------------------------------------------------------------|------|
| TC-16    | 开发端口自动回退 | 保持一个现有开发实例占用 `3000`，在第二个终端再次执行 `npm run dev`。       | 第二个实例不会崩溃；终端明确打印新的可用地址（如 `http://localhost:3001`）。 | [ ]  |
| TC-17    | 缺失 AI Key 启动 | 删除或暂时重命名本地 `.env` 后执行 `npm run dev`，并在浏览器中打开首页。 | 首页和基础文件处理功能仍能正常渲染；AI 助手会提示需要配置 `GEMINI_API_KEY`、`OPENAI_API_KEY`、`DEEPSEEK_API_KEY` 中的至少一个，但图片 OCR 仍可继续工作，只要本地 PaddleOCR runtime 已安装。 | [ ]  |
| TC-17A   | 本地 OCR bootstrap 缺失 | 删除或临时重命名 `.local/paddleocr/` 后，在浏览器中对图片执行 OCR。 | 界面应明确提示需要安装 Python `3.9+` 并重跑 `npm install` 或 `npm run setup:ocr`，而不是提示 AI key 缺失。 | [ ]  |
| TC-18    | 生产环境运行时注入 | 在已构建产物上设置运行时 `GEMINI_API_KEY`、`OPENAI_API_KEY` 或 `DEEPSEEK_API_KEY`，通过 `npm run start` 启动服务并访问 `/api/runtime-config`。 | 服务返回的是“哪些 provider 已配置”以及“本地 OCR runtime 是否就绪”的摘要，而不是原始 key；AI 助手不依赖把 key 预先写死进构建产物。 | [ ]  |
| TC-19    | 多 Provider 顺序回退 | 同时配置多个 AI key，其中首个 provider 故意使用无效 key，后一个 provider 使用有效 key，再触发 AI 助手。 | 系统会跳过前一个失败 provider，自动回退到后一个可用 provider，且界面无需手动切换模型。图片 OCR 不参与这条回退链，因为它已改为本地 PaddleOCR。 | [ ]  |
