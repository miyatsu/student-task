# 测试文档

为了保证这套轻量级但功能复杂的多功能文件处理逻辑保持高度可用性，应当在版本迭代之前经历以下测试。当前重点为主干流程的 E2E（在浏览器前台）以及人工边界探测验证。

## 1. 测试策略
鉴于系统的纯前端运行特点：
1. **组件层面单元测试**：针对某些纯数据重组类型的 Utils 或者 Hook 做基本断言；由于此项目目前属于紧凑全双工形式，并没有细分过多纯工具，单元自动化覆盖放在二线。
2. **浏览器 E2E 原理验证**：保障 Canvas 操作不产生跨域死锁或者内存逸出（OOM）。
3. **集成验证**：重点在跨模块之间流转的文件，例如是否在不同区域依然保持正确的名字以及拓展后缀名。这需要人工 Checklist 校验。

## 2. 人工测试大纲及执行 Check List

开发者或测试人员按以下标准开展预发布检验。

### 模块一：全局文件组织 (DND与排序)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-01    | 类型混合空降 | 同时按住一本 PDF，一张 `jpg`，一个 `docx` 后，拖放至 App 工作虚线框中。          | 文件必须顺利分发到对应的 3 个独立的（Image/PDF/Word）区块内部并提供对应缩略状态。 | [ ]  |
| TC-02    | 跨文件删除   | 在各个分类中分别选中若干文件，点击红色的清空/删除选定。                         | 仅仅只有那些 Checkbox=true 的文件在试图内消失，其余保留不变。                      | [ ]  |
| TC-03    | 全局排序切换 | 点击文字“SIZE (大小)”标签的排序按钮。                                            | 组内按从达到小升序排列对应的大小展示；点击第二次，改为升序或者降序，方向图标更改。 | [ ]  |

### 模块二：AI 图像超级强化处理 (`ImageEnhanceModal`)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-04    | 特大尺寸载入 | 上传并点击 4K 以上分辨率或 5MB 以上图片的魔棒特效。                              | 由于 Patch-Size 的保护，等待虽然漫长（超 30s），但浏览器不得崩溃或者显示白屏抛出异常。 | [ ]  |
| TC-05    | 滑块边界检测 | 在完成增强的对比状态下，用鼠标将游标滑至最左方边界并停止，再滑动至右侧边缘并松开。 | 左方停止时应完全展示原图（含水印），右方全幅显示增强质感。不应该因为游标脱模影响布局。  | [ ]  |
| TC-06    | 中断生命周期 | AI 模型处于 Loading 状态（旋转缓冲），立刻点击底界面的 "Cancel" 中止窗口组件。     | 等待数秒不应阻碍程序的正常销毁；模型推理的上下文被回收且在原页面内无任何不兼容假死。 | [ ]  |

### 模块三：PDF 工作台互操作 (`PdfEditor`)
参阅：`[src/components/PdfEditor.tsx](../src/components/PdfEditor.tsx)`
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-07    | PDF 单页删除 | 打开编辑抽屉，点击指定页面上的叉号。                                              | 剩下页码实时重构为缩略图；点击导出生成一个只有其余页面的完全可读新 PDF 文件。   | [ ]  |

### 模块四：压缩集成与导出 (Zip 机制)
| 用例编号 | 用例名称     | 操作动作                                                                        | 预期结果                                                                        | 状态 |
|----------|-------------|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------|
| TC-08    | 复合混装导出 | 勾选 1 个经过重度模糊增强而复制的副本图像、1 个拆分的 PDF 和 1 份 Word 文件并打包。| 生成压缩包不报错；解包后打开 3 份文件在系统自带预览中不会由于文件头损坏而不能显示。 | [ ]  |

### 模块五：开发环境与端口冲突回归
| 用例编号 | 用例名称         | 操作动作                                                                 | 预期结果                                                                 | 状态 |
|----------|------------------|--------------------------------------------------------------------------|--------------------------------------------------------------------------|------|
| TC-09    | 开发端口自动回退 | 保持一个现有开发实例占用 `3000`，在第二个终端再次执行 `npm run dev`。       | 第二个实例不会崩溃；终端明确打印新的可用地址（如 `http://localhost:3001`）。 | [ ]  |
| TC-10    | 缺失 Gemini Key 启动 | 删除或暂时重命名本地 `.env` 后执行 `npm run dev`，并在浏览器中打开首页。 | 首页和基础文件处理功能仍能正常渲染；只有 AI 助手和图片 OCR 提示需要配置 `GEMINI_API_KEY`。 | [ ]  |
| TC-11    | 生产环境运行时注入 | 在已构建产物上设置运行时 `GEMINI_API_KEY`，通过 `npm run start` 启动服务并访问 `/api/runtime-config`。 | 服务返回当前运行环境中的 Key；AI 功能不依赖把 Key 预先写死进构建产物。 | [ ]  |
