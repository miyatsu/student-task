# 用户手册

## 项目简介

欢迎使用**多功能文件处理系统**！本系统提供了一个开箱即用、富交互的可视化界面，使用户能够在浏览器中处理各种日常文件（PDF、图片及Word文档），甚至利用本地及云端的人工智能进行强化。

本系统特色在于**重度依赖纯前端本地处理（如通过浏览器计算单元提升图片分辨率、修改PDF）**，保护您的隐私数据，无需上传服务器。

## 核心功能指南

### 1. 文件的导入、分类与操作
- **导入文件**：直接拖拽各类文件到虚线框内，或点击上传。系统会自动把文件归档到“图片”、“PDF”或者“Word”栏目中。
- **文件排序与拖拽（Dnd）**：不同类型的文件可以点击右上角的标签按照“名字”、“日期”、“大小”排序，也可以在拖拽手柄上长按并调整顺序。
- **文档的通用操作**：鼠标悬停在具体文件上，可以使用提供的功能进行：**复制（克隆）、重新命名、预览**。

### 2. 人工智能图像增强 (AI Upscaler)
这可能是本系统最炫酷的功能之一：当图片模糊时，悬停在图片上点击“魔棒”图标。
- 随后会弹出一个 **[AI图像增强窗口](./implementation.md#ai-image-enhancement)**。
- 您稍微等待几秒（受限于当前电脑的显卡与CPU性能），系统会在本地下载一个模型并在后台计算，将照片进行细节补充和智能重绘。
- 使用中央的滑动条左右对比，点击**保存**即可将其存为独立的高清副本。

### 3. PDF 自定义拆分、合并与编辑
- 点击 PDF 文件的“编辑”图标即可进入 PDF 工作台。
- 在里面您可以自由删除不需要的页面，也可以从外部提取部分页面或全部合并产生新的 PDF 文档。
- 本系统使用本地级 [PDF-lib](./architecture.md#frontend-tech) 完成此类计算。

### 4. 万能的智能助手 (Gemini AI Chat)
- 勾选任意多个文件（可跨越 PDF，文字等类型），点击右下角悬浮棒或者工具栏的 **AI 助手**。
- 您随时可以像往常和人类交流一样，向助手询问这些被选中文件的相关知识，或让它帮忙总结、翻译和分析。
- 如果你是在本地开发环境中运行该项目，而不是在 Google AI Studio 的托管环境里，请先前往 <https://aistudio.google.com/app/apikey> 申请独立的 Gemini API Key，再在项目根目录配置 `.env` 中的 `GEMINI_API_KEY`。未配置时，助手窗口会直接提示如何启用 Gemini，而不会让整个页面失效。

### 5. 图片文字提取（Gemini OCR）
- 在图片条目上点击“扫描文字”按钮，可以把图片中的文本提取为 Markdown 文件下载到本地。
- 该能力同样依赖 `GEMINI_API_KEY`。如果本地未配置密钥，界面会弹出提示，引导你先去 Google AI Studio 申请 Key 并完成环境变量配置。

### 6. 一键打包压缩下载
- 勾选您需要的文件（若全部文件则不需勾选，可直接全选），点击 **导出选中的内容 (Zip)**，系统会自动把所有零散文件归档至一个 ZIP 压缩包下载到本地。

---

*如需进一步了解系统设计需求，请参考[需求分析文档](./requirements.md)。想要了解本产品实现细节，可阅读[技术与实现文档](./implementation.md)。*
