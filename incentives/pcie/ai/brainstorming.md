- [ ] AI 功能
- [ ] OCR 表格功能
- [ ] OCR 功能：（1）大图缩放不能影响 OCR 识别效果（2）不需要为 OCR 设置超时，但要为用户添加进度提醒、已用时、预计用时等信息，并允许用户随时中断识别。请为此设计简洁、大方的 UI，并实现相应的功能。同步更新文档。完成后，git push。（2）OCR 要实时输出识别结果，比如可以弹出一个轻量的文本框，实时增量显示识别的文字、表格等结果，并提供一个一键复制识别结果的功能。同步更新文档。完成后，git push。
- [x] docs：请检查 docs/ 下文档是否包含软件工程规范所要求的 4 + 1 视图？如果没有，请添加该部分内容，并在后续持续保持更新，请使用 UML 作图。完成后，git push。
- [ ] "本机没有 plantuml CLI，但有 Java 和 Graphviz；我会按项目文档里的 fallback，用临时 PlantUML jar 渲染 SVG，不把 jar 放进仓库。"：请将本项目文档的生成/写作环境也作为系统的一部分进行交付（请自行选择最佳打包、安装、交付方式），使得开发人员可以顺畅地进行文档更新与写作。同步更新相关文档。完成后，git push。